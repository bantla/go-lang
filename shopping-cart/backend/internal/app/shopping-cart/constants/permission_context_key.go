package constants

const (
	// PermissionContextKeyValidPermission is used when using the echo request context to obtain the valid permission data
	PermissionContextKeyValidPermission = "ValidPermission"

	// PermissionContextKeyPermissionService is used when using the echo request context to obtain the role service
	PermissionContextKeyPermissionService = "WithPermissionService"
)
